[project]
name = "yolo11-detector"
version = "1.0.0"
description = "YOLO11 Object Detection with Streamlit"
requires-python = ">=3.8"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
